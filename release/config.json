{
  "maintainer": "<coldchain@interbloc.org>",
  "version": "0.1.0",
  "description": "A repository of binaries for Cosmos SDK blockchains",
  "suites": {
    "stable": {
      "build": "goleveldb",
      "description": "Ledger-compatible client and daemon"
    },
    "mev": {
      "build": "goleveldb-mev",
      "description": "Ledger-compatible client and daemon with mev-tendermint"
    },
    "experimental": {
      "build": "pebbledb",
      "description": "Client and daemon with pebbledb"
    },
    "experimental_mev": {
      "build": "pebbledb-mev",
      "description": "Client and daemon with pebbledb and mev-tendermint"
    }
  }
}
